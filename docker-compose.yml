services:
  app:
    build: .
    ports:
      - "8501:8501"
    environment:
      - OPENAI_API_KEY=${OPENAI_API_KEY}
      - QDRANT_CLOUD_URL=${QDRANT_CLOUD_URL}
      - QDRANT_API_KEY=${QDRANT_API_KEY}
